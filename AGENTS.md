<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read relevant guide in `node_modules/next/dist/docs/` (resolved from this file's dir; in monorepos `next` package may not be visible from repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from diff only re-creates uncommitted change; committing it w/ your work keeps tree clean.

<!-- END:nextjs-agent-rules -->
